const fs = require('fs');
const path = require('path');
const db = require('') // require where the information for pool is

// test json file
// const testJsonFile = path.resolve(__dirname, '') // location of test json file 

describe('Testing Database', () => {

});